CHIP Or32Way {
    IN in[32];
    OUT out;

    PARTS:
    Or8Way(in=in[0..7], out=o1);
    Or8Way(in=in[8..15], out=o2);
    Or8Way(in=in[16..23], out=o3);
    Or8Way(in=in[24..31], out=o4);

    Or(a=o1, b=o2, out=o12);
    Or(a=o3, b=o4, out=o34);
    Or(a=o12, b=o34, out=out);
}
