CHIP Mux4Way32 {
    IN a[32], b[32], c[32], d[32], sel[2];
    OUT out[32];

    PARTS:
    Mux32(a=a, b=b, sel=sel[0], out=ab);
    Mux32(a=c, b=d, sel=sel[0], out=cd);
    Mux32(a=ab, b=cd, sel=sel[1], out=out);
}
